# QuirkLog 每日计划应用程序
# 打包和分发所需的依赖

# 核心打包依赖
pyinstaller==5.13.2
auto-py-to-exe==2.40.0

# 应用依赖 (虽然主要使用标准库，但为了确保兼容性)
Pillow==10.0.0

# 用于创建安装程序的工具
cx-Freeze==6.15.10

# 开发和构建工具
setuptools==68.2.2
wheel==0.41.2
