# 每日计划与总结应用程序的依赖
# tkinter 通常随Python标准库安装，不需要额外安装

# 定时任务相关依赖
schedule==1.2.0        # 定时任务调度器
openai==1.54.3         # OpenAI客户端（兼容OpenRouter）

# 可选的增强依赖
# python-dateutil==2.8.2  # 更好的日期时间处理
# ujson==5.8.0           # 更快的JSON处理

# 主要使用Python标准库，保持轻量级
